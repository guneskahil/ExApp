<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.pagesix">

    <Button
        android:id="@+id/exUrunSayfasiButton"
        android:layout_width="204dp"
        android:layout_height="60dp"
        android:onClick="exUrunSayfasi"
        android:text="Tarihi Geçen Ürünler"
        android:textColor="#FDFDFD"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/exListe"
        android:layout_width="408dp"
        android:layout_height="209dp"
        tools:ignore="MissingConstraints"
        tools:layout_editor_absoluteX="2dp"
        tools:layout_editor_absoluteY="50dp" />

    <Button
        android:id="@+id/urunSayfasiButton"
        android:layout_width="204dp"
        android:layout_height="60dp"
        android:onClick="urunSayfasi"
        android:text="Ürünler"
        android:textColor="#FDFDFD"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/settingButton"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginTop="268dp"
        android:onClick="settingButton"
        android:layout_marginLeft="345dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/exListe"
        app:srcCompat="@drawable/ic_baseline_settings_24" />

</androidx.constraintlayout.widget.ConstraintLayout>